# 依赖目录
node_modules/
*/node_modules/

# 日志文件
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 运行时数据
pids
*.pid
*.seed
*.pid.lock

# 目录
.npm
.eslintcache
.node_repl_history
.yarn-integrity

# 环境变量文件
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 编辑器配置
.idea
.vscode
*.swp
*.swo

# 操作系统文件
.DS_Store
Thumbs.db

# 用户数据文件
# server/data/*.json # Commented out as server/data/ below is more general
data/
# Ignore all contents of server/data, but not the directory itself
server/data/*
# Un-ignore the specific config file
!server/data/web3-live-config.json
# Ensure the server/data directory itself is not ignored if it was previously by a broader rule
# (though server/data/* should handle this for files within, this makes it explicit for the dir if needed)
# !server/data/
# The above line is usually not needed if server/data/* is used and you have an un-ignored file inside.
# We will rely on server/data/* and !server/data/web3-live-config.json
# SpecStory explanation file
.specstory/.what-is-this.md
